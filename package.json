{
  "name": "gappi",
  "version": "1.0.0",
  "description": "Gappi is realtime chat application built with NextJS, NestJS(backend), Mongodb.",
  "main": "index.js",
  "repository": "https://github.com/iSatVeerSingh/gappi.git",
  "author": "SatVeer Singh <developer.satveer@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=16.17.0",
    "yarn": ">=1.22.0",
    "npm": "Please use yarn"
  },
  "scripts": {
    "prepare": "husky install",
    "build:frontend": "yarn --cwd ./frontend/ build",
    "build": "yarn build:frontend",
    "dev:frontend": "yarn --cwd ./frontend/ dev",
    "lint:frontend": "yarn --cwd ./frontend/ lint",
    "lint": "yarn lint:frontend"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "husky": "^8.0.1",
    "prettier": "^2.7.1"
  }
}
